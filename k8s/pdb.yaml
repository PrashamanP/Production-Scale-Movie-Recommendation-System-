apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: movie-recommender-pdb
spec:
  minAvailable: 2  # Always keep at least 2 pods running during disruptions
  selector:
    matchLabels:
      app: movie-recommender
